version: 2.1
jobs:
  build:
    docker:
      - image: ubuntu:22.04
    steps:
      - checkout

      - restore_cache:
          keys:
            - sfml-build-2.6.1-v1

      - run:
          name: "Install Dependencies"
          command: |
            apt-get update
            apt-get install -y \
                build-essential wget gpg \
                libgl1-mesa-dev libxrandr-dev libx11-dev libxcursor-dev \
                libudev-dev libopenal-dev libflac-dev libvorbis-dev libegl1-mesa-dev \
                libfreetype6-dev xvfb

            # CMake setup
            wget -O - https://apt.kitware.com/keys/kitware-archive-latest.asc | gpg --dearmor -o /usr/share/keyrings/kitware-archive-keyring.gpg
            echo 'deb [signed-by=/usr/share/keyrings/kitware-archive-keyring.gpg] https://apt.kitware.com/ubuntu/ jammy main' | tee /etc/apt/sources.list.d/kitware.list >/dev/null
            
            apt-get update
            apt-get install -y cmake

      - run:
          name: "Build and Install SFML"
          command: |
            SFML_VERSION="2.6.1"
            
            # Only build if the directory doesn't exist (cache miss)
            if [ ! -d "SFML-${SFML_VERSION}" ]; then
                echo "Building SFML from source..."
                wget https://github.com/SFML/SFML/archive/refs/tags/${SFML_VERSION}.tar.gz
                tar -xzf ${SFML_VERSION}.tar.gz
                cd SFML-${SFML_VERSION}
                cmake .
                make -j$(nproc)
                cd ..
            else
                echo "Using cached SFML build..."
            fi

            # Always run install (fast if already compiled)
            cd SFML-${SFML_VERSION}
            make install
            ldconfig

      - save_cache:
          paths:
            - SFML-2.6.1
          key: sfml-build-2.6.1-v1

      - run:
          name: "Build and Run Project"
          command: |
            mkdir -p build
            cd build
            cmake ..
            # Optimization: Compile your project in parallel
            make -j$(nproc)
            
            xvfb-run timeout 5s ./pacman || if [ $? = 124 ]; then exit 0; else exit $?; fi

workflows:
  build-and-run:
    jobs:
      - build