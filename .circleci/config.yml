version: 2.1
jobs:
  build:
    docker:
      - image: ubuntu:22.04 # Use a base Ubuntu image
    steps:
      - checkout # Checks out your code
      - run:
          name: "Install Dependencies"
          command: |
            apt-get update
            # Install prerequisites for adding a new repository and for building
            apt-get install -y build-essential wget gpg

            # Add Kitware's APT repository to get a newer version of CMake
            wget -O - https://apt.kitware.com/keys/kitware-archive-latest.asc | gpg --dearmor -o /usr/share/keyrings/kitware-archive-keyring.gpg
            echo 'deb [signed-by=/usr/share/keyrings/kitware-archive-keyring.gpg] https://apt.kitware.com/ubuntu/ jammy main' | tee /etc/apt/sources.list.d/kitware.list >/dev/null
            
            # Update package list again and install cmake and gtest
            apt-get update
            apt-get install -y cmake libgtest-dev
      - run:
          name: "Build and Run"
          command: |
            mkdir -p build
            cd build
            cmake ..
            make
            ./Pacman
workflows:
  build-and-run:
    jobs:
      - build